name: Build and Deploy code

on: [push, pull_request]

jobs:
  job1:
    name: Run on Ubuntu
    runs-on: ubuntu-22.04
#    environment:
#      name: ENV
    env:
      DB_PORT: 5432 # pytest упал, потому что pydantic не пропустил
      DB_NAME: ${{secrets.DB_NAME}}
      DB_PASSWORD: ${{secrets.DB_PASSWORD}}
      DB_USERNAME: ${{secrets.DB_USERNAME}}
      SECRET_KEY: ${{secrets.SECRET_KEY}}
      ALGORITHM: ${{secrets.ALGORITHM}}
      ACCESS_TOKEN_EXP_MIN: 30 # pytest упал, потому что pydantic не пропустил
      DB_HOST: localhost #  клиент пытается подключиться через сокет, а не по TCP. Нужно явно указать
      POSTGRES_USER: ${{vars.POSTGRES_USER}}
      POSTGRES_PASSWORD: ${{vars.POSTGRES_PASSWORD}}
      POSTGRES_DB: ${{vars.POSTGRES_DB}}
    services:
      postgres:
        image: postgres:16
        ports:
          - 5432:5432
        env:
          POSTGRES_USER: postgres # необходимо явно прописывать юзер, пароль и дб
          POSTGRES_PASSWORD: $POSTGRES_PASSWORD # переменные на github actions не работают.
          POSTGRES_DB: fastapi_test # запуск через переменную $POSTGRES_DB pytest упал с ошибкой
        options: >-
          --health-cmd="pg_isready -U $POSTGRES_USER"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - name: test with pytest
        run: |
          pip install pytest
          pytest

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        id: buildx
      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v5
        with:
          context: ./
          file: ./Dockerfile
          builder: ${{steps.buildx.outputs.name}}
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME  }}/proj-vkhehay:latest
      - name: Image digest
        run: echo ${{steps.docker_build.outputs.digest}}







#name: GitHub Actions Demo
#run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
#on: [push] # run action on every push and pull requests
#  push: # run on push
#    branches: # run only in specified branch(ies)
#      - "main" # run when push in main branch
#      - "proj-working" # or run when push in proj-working branch
#  pull_request: # run on pull_request
#    branches: # run in specified branch(ies)
#      - "test-branch" # run when pull in test-branch
#jobs:
#  Explore-GitHub-Actions:
#    runs-on: ubuntu-latest
#    steps:
#      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
#      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
#      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
#      - name: Check out repository code
#        uses: actions/checkout@v5
#      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
#      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#      - name: List files in the repository
#        run: |
#          ls ${{ github.workspace }}
#      - run: echo "🍏 This job's status is ${{ job.status }}."